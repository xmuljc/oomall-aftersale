<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.xmu.aftersale.dao.mapper.AfterSalesOrderMapper">

    <!-- 显式结果映射 -->
    <resultMap id="BaseResultMap" type="cn.edu.xmu.aftersale.bo.AfterSalesOrderBO">
        <id property="id" column="id"/>
        <result property="shopId" column="shop_id"/>
        <result property="customerId" column="customer_id"/>
        <result property="orderId" column="order_id"/>
        <result property="type" column="type"/>
        <result property="status" column="status"/>
        <result property="conclusion" column="conclusion"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, shop_id, customer_id, order_id, type, status, conclusion, create_time, update_time
        FROM after_sales_order
        WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE after_sales_order
        SET status = #{bo.status},
            type   = #{bo.type},
            conclusion = #{bo.conclusion},
            update_time = NOW()
        WHERE id = #{bo.id}
    </update>

</mapper>